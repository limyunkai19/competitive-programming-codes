// use the following code for faster IO in C++
// helps when the time limit is tight
//
// note: the code is proven faster than scanf/printf

#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    return 0;
}
